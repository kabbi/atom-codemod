/**
 * TODO: use some babel package that adapts to running nodejs version, i. e. not
 * transform everything to es5 when current node supports es6 already
 */
require('babel-register');
require('./main');
